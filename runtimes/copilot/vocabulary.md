# Copilot Vocabulary

**Moved.** See `vocabularies/Copilot.hw` for the current symbol list.

All vocabulary definitions live in `vocabularies/*.hw` files — the sole namespace authority. This file is kept as a redirect only.
